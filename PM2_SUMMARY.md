# PM2 Configuration Summary

## ✅ Что было исправлено в `ecosystem.config.js`

### До (проблемы):
1. ❌ `env_file` указывал на `./.env` относительно корня (неправильный путь)
2. ❌ Не было переменной `FRONTEND_URL` для production
3. ❌ Не было логирования ошибок
4. ❌ `fork` mode вместо `cluster` для production
5. ❌ Не было мониторинга памяти и автоперезапуска

### После (исправлено):
1. ✅ Убран `env_file`, переменные прописаны в `env` и `env_production`
2. ✅ Добавлена `FRONTEND_URL` для backend (dev и prod)
3. ✅ Настроено логирование в `backend/logs/` и `frontend/logs/`
4. ✅ Включен `cluster` mode для масштабируемости
5. ✅ Добавлен `max_memory_restart`, `autorestart`, `max_restarts`
6. ✅ Все переменные окружения настроены для dev и prod

---

## 📋 Изменения в конфигурации

### Backend (pokerscrum-backend)

**Было:**
```javascript
{
  name: 'pokerscrum-backend',
  cwd: './backend',
  script: './dist/main.js',
  env_file: './.env',
  env: { NODE_ENV: 'development' },
  env_production: { NODE_ENV: 'production' }
}
```

**Стало:**
```javascript
{
  name: 'pokerscrum-backend',
  cwd: './backend',
  script: './dist/main.js',
  
  // Production настройки
  instances: 1,
  exec_mode: 'cluster',
  autorestart: true,
  max_restarts: 10,
  min_uptime: '10s',
  max_memory_restart: '500M',
  
  // Логирование
  error_file: './logs/backend-error.log',
  out_file: './logs/backend-out.log',
  
  // Переменные окружения
  env: {
    NODE_ENV: 'development',
    PORT: 3000,
    FRONTEND_URL: 'http://localhost:3001'
  },
  env_production: {
    NODE_ENV: 'production',
    PORT: 3000,
    FRONTEND_URL: 'https://pokerscrum.ru'
  }
}
```

### Frontend (pokerscrum-frontend)

Аналогичные изменения + добавлены переменные Nuxt:

```javascript
env_production: {
  NODE_ENV: 'production',
  PORT: 3001,
  NITRO_PORT: 3001,
  NUXT_PUBLIC_API_BASE: 'https://pokerscrum.ru',
  NUXT_PUBLIC_WS_BASE: 'wss://pokerscrum.ru'
}
```

---

## 🆕 Новые файлы

### Документация
1. **PM2_DEPLOYMENT.md** - полное руководство по деплою через PM2
   - Команды PM2
   - Настройка переменных окружения
   - Автозапуск
   - Мониторинг
   - Устранение проблем

2. **PM2_CHECKLIST.md** - быстрый чеклист для деплоя
   - Пошаговый список действий
   - Проверки перед запуском
   - Тестирование
   - Быстрые команды

3. **PM2_SUMMARY.md** - этот файл (резюме изменений)

### Скрипты
1. **prepare-pm2.sh** (Linux/macOS) - скрипт подготовки к запуску
   - Создает директории для логов
   - Проверяет установку PM2
   - Проверяет наличие сборок
   - Проверяет .env файлы

2. **prepare-pm2.bat** (Windows) - то же самое для Windows

### Обновления
1. **.gitignore** - добавлены директории логов PM2
2. **README.md** - добавлен раздел про PM2

---

## 🚀 Как использовать

### Первый запуск

```bash
# 1. Подготовка (Linux/macOS)
chmod +x prepare-pm2.sh
./prepare-pm2.sh

# Или Windows
prepare-pm2.bat

# 2. Сборка приложений
cd backend && npm run build:prod
cd ../frontend && npm run build:prod

# 3. Запуск PM2
pm2 start ecosystem.config.js --env production

# 4. Проверка
pm2 status
pm2 logs

# 5. Автозапуск (опционально)
pm2 startup
pm2 save
```

### Обновление приложения

```bash
# 1. Получить обновления
git pull origin main

# 2. Установить зависимости
cd backend && npm install
cd ../frontend && npm install

# 3. Собрать
cd backend && npm run build:prod
cd ../frontend && npm run build:prod

# 4. Перезапустить PM2 (0-downtime)
cd ..
pm2 reload all
```

---

## 🔑 Ключевые особенности

### Cluster Mode
- Backend и Frontend запускаются в cluster mode
- Можно увеличить `instances` до `'max'` для использования всех ядер CPU
- Автоматический балансировщик нагрузки

### Автоперезапуск
- При превышении лимита памяти (`max_memory_restart`)
- При падении приложения (`autorestart: true`)
- Ограничение на количество перезапусков (`max_restarts: 10`)

### Логирование
- Раздельные файлы для ошибок и вывода
- Timestamp в логах
- Автоматическая ротация (можно настроить с `pm2-logrotate`)

### Переменные окружения
- Разные конфигурации для dev и prod
- Секретные данные в `.env` файлах (не в ecosystem.config.js)
- Легкое переключение: `--env development` или `--env production`

---

## 🎯 Преимущества новой конфигурации

### Для разработки
- ✅ Легко запустить локально: `pm2 start ecosystem.config.js --env development`
- ✅ Все переменные окружения правильно настроены
- ✅ CORS работает из коробки

### Для production
- ✅ Cluster mode для высокой производительности
- ✅ Автоматический restart при сбоях
- ✅ Мониторинг использования памяти
- ✅ Полное логирование
- ✅ Правильные production URL

### Для DevOps
- ✅ Простой деплой через `pm2 reload all`
- ✅ 0-downtime обновления
- ✅ Логи в отдельных файлах
- ✅ Автозапуск при перезагрузке сервера

---

## 📊 Сравнение режимов

| Режим | BACKEND_URL | FRONTEND_URL | CORS | Использование |
|-------|------------|--------------|------|---------------|
| **Development** | localhost:3000 | localhost:3001 | ✅ | Локальная разработка |
| **Production** | pokerscrum.ru | pokerscrum.ru | ✅ | Production сервер |

---

## 🔧 Дополнительная настройка

### Масштабирование (использование всех ядер)

В `ecosystem.config.js` измените:
```javascript
instances: 'max', // Вместо 1
```

### Настройка логирования

Установите `pm2-logrotate` для автоматической ротации логов:
```bash
pm2 install pm2-logrotate
pm2 set pm2-logrotate:max_size 10M
pm2 set pm2-logrotate:retain 7
```

### Мониторинг с PM2 Plus

```bash
# Регистрация на pm2.io
pm2 link <secret_key> <public_key>

# Теперь можно мониторить приложение через веб-интерфейс
```

---

## ✅ Чеклист готовности

Перед запуском на production убедитесь:

- [ ] `ecosystem.config.js` настроен правильно
- [ ] Созданы директории `backend/logs/` и `frontend/logs/`
- [ ] Backend собран: `backend/dist/main.js` существует
- [ ] Frontend собран: `frontend/.output/server/index.mjs` существует
- [ ] `.env` файлы содержат правильные значения
- [ ] База данных настроена и миграции применены
- [ ] PM2 установлен глобально
- [ ] Nginx настроен (если используется)

---

## 📚 Полезные ссылки

- [PM2 Documentation](https://pm2.keymetrics.io/docs/)
- [PM2 Cluster Mode](https://pm2.keymetrics.io/docs/usage/cluster-mode/)
- [PM2 Startup Script](https://pm2.keymetrics.io/docs/usage/startup/)
- [PM2 Plus Monitoring](https://pm2.io/)

---

## 🎉 Итог

Конфигурация PM2 теперь полностью готова к использованию как для разработки, так и для production. Все переменные окружения настроены правильно, CORS работает, логирование включено, и есть защита от падений.

**Запуск на production:**
```bash
pm2 start ecosystem.config.js --env production
pm2 startup
pm2 save
```

Готово! 🚀
