# Production Environment Variables для Planning Poker
# Этот файл используется при сборке production версии

# ⚠️ ВАЖНО: Для Nginx reverse proxy используйте ПУСТЫЕ значения!
# Nginx проксирует /api/ на backend, поэтому нужны относительные пути

# API Backend URL (пустая строка = относительные пути)
NUXT_PUBLIC_API_BASE=

# WebSocket URL (пустая строка = определяется браузером)
NUXT_PUBLIC_WS_BASE=

# Server Ports
PORT=3001
NITRO_PORT=3001

# Node Environment
NODE_ENV=production
