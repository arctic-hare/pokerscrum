# 🚀 Быстрый деплой HOTFIX

## Проблема
❌ Production сайт пытается подключиться к `localhost` → запрос разрешения на доступ к локальной сети

## Решение
✅ Исправлен код в `frontend/stores/game.ts` - убран fallback на localhost

---

## ⚡ Шаги для деплоя (СРОЧНО)

### На production сервере:

```bash
# 1. Перейти в директорию проекта
cd /path/to/planning-poker

# 2. Получить последние изменения
git pull origin main

# 3. Перейти в frontend
cd frontend

# 4. ⚠️ ВАЖНО: Собрать с production переменными!
npm run build:prod

# 5. Вернуться в корень
cd ..

# 6. Перезапустить frontend через PM2
pm2 reload pokerscrum-frontend

# 7. Проверить, что всё работает
pm2 logs pokerscrum-frontend --lines 20
```

---

## ✅ Проверка после деплоя

1. **Откройте сайт:** https://pokerscrum.ru
2. **НЕ должно быть** запроса на доступ к локальной сети
3. **Откройте DevTools** → Network → WS
4. **Проверьте WebSocket URL:** должно быть `wss://pokerscrum.ru/ws`

---

## 🔍 Если проблема осталась

### Проверьте, что использовались правильные команды:

```bash
# ✅ ПРАВИЛЬНО:
npm run build:prod

# ❌ НЕПРАВИЛЬНО:
npm run build
```

### Проверьте переменные в сборке:

```bash
# Временно создайте файл frontend/pages/test-env.vue:
```
```vue
<template>
  <div>
    <h2>Runtime Config</h2>
    <pre>{{ JSON.stringify(config.public, null, 2) }}</pre>
  </div>
</template>

<script setup>
const config = useRuntimeConfig();
</script>
```

Откройте `https://pokerscrum.ru/test-env` и проверьте:
- `apiBase`: должно быть `"https://pokerscrum.ru"`
- `wsBase`: должно быть `"wss://pokerscrum.ru"`

**Если видите localhost - значит сборка была с неправильными переменными!**

После проверки удалите файл:
```bash
rm frontend/pages/test-env.vue
```

---

## 🆘 Если всё ещё не работает

### Вариант 1: Полная пересборка

```bash
cd frontend

# Очистить кеш
rm -rf .nuxt
rm -rf .output
rm -rf node_modules/.cache

# Пересобрать
npm run build:prod

# Перезапустить
cd ..
pm2 reload pokerscrum-frontend
```

### Вариант 2: Установить переменные вручную

```bash
cd frontend

# Сборка с явным указанием переменных
cross-env NUXT_PUBLIC_API_BASE=https://pokerscrum.ru \
         NUXT_PUBLIC_WS_BASE=wss://pokerscrum.ru \
         nuxt build

cd ..
pm2 reload pokerscrum-frontend
```

### Вариант 3: Использовать PM2 конфигурацию

```bash
# Остановить
pm2 delete pokerscrum-frontend

# Запустить с правильными переменными из ecosystem.config.js
pm2 start ecosystem.config.js --only pokerscrum-frontend --env production

# Сохранить
pm2 save
```

---

## 📋 Итоговый чеклист

- [ ] git pull выполнен
- [ ] Frontend собран с `npm run build:prod`
- [ ] PM2 перезапущен (`pm2 reload pokerscrum-frontend`)
- [ ] Логи проверены (нет ошибок)
- [ ] Сайт открывается
- [ ] НЕТ запроса на доступ к локальной сети
- [ ] WebSocket подключается к `wss://pokerscrum.ru/ws`
- [ ] Можно создать игру
- [ ] Можно проголосовать
- [ ] Всё работает!

---

## 📝 Подробная документация

Детали: [HOTFIX_LOCALHOST_ISSUE.md](HOTFIX_LOCALHOST_ISSUE.md)

---

**ВРЕМЯ ДЕПЛОЯ:** ~2-3 минуты  
**ПРИОРИТЕТ:** 🔴 CRITICAL  
**ДАУНТАЙМ:** 0 секунд (используется `pm2 reload`)
