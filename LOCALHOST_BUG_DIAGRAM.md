# Диаграмма проблемы с localhost 🐛

## ❌ Что происходило ДО исправления

```
┌─────────────────────────────────────────────────────┐
│  Production Server                                   │
│                                                      │
│  Frontend собран с:                                 │
│  ❌ npm run build                                    │
│  (использует .env с localhost)                      │
│                                                      │
│  ┌────────────────────────────────────────┐         │
│  │  Nuxt Build (.output/)                 │         │
│  │                                         │         │
│  │  Runtime Config:                       │         │
│  │  ❌ apiBase: "http://localhost:3000"   │         │
│  │  ❌ wsBase: "ws://localhost:3000"      │         │
│  └────────────────────────────────────────┘         │
│                                                      │
└──────────────────────┬───────────────────────────────┘
                       │
                       │ Deployed to
                       │
                       ▼
         ┌─────────────────────────────┐
         │  https://pokerscrum.ru      │
         └──────────┬──────────────────┘
                    │
                    ▼
         ┌──────────────────────────────┐
         │  User Browser                 │
         │                               │
         │  WebSocket tries to connect:  │
         │  ❌ ws://localhost:3000/ws    │
         │                               │
         │  Browser shows:               │
         │  ⚠️  "pokerscrum.ru          │
         │      запрашивает разрешение  │
         │      на доступ к локальной   │
         │      сети"                   │
         └───────────────────────────────┘
```

---

## ✅ После исправления

```
┌─────────────────────────────────────────────────────┐
│  Production Server                                   │
│                                                      │
│  Frontend собран с:                                 │
│  ✅ npm run build:prod                               │
│  (использует production переменные)                 │
│                                                      │
│  ┌────────────────────────────────────────┐         │
│  │  Nuxt Build (.output/)                 │         │
│  │                                         │         │
│  │  Runtime Config:                       │         │
│  │  ✅ apiBase: "https://pokerscrum.ru"   │         │
│  │  ✅ wsBase: "wss://pokerscrum.ru"      │         │
│  └────────────────────────────────────────┘         │
│                                                      │
└──────────────────────┬───────────────────────────────┘
                       │
                       │ Deployed to
                       │
                       ▼
         ┌─────────────────────────────┐
         │  https://pokerscrum.ru      │
         └──────────┬──────────────────┘
                    │
                    ▼
         ┌──────────────────────────────┐
         │  User Browser                 │
         │                               │
         │  WebSocket connects to:       │
         │  ✅ wss://pokerscrum.ru/ws    │
         │                               │
         │  ✅ No permission prompt      │
         │  ✅ WebSocket connected       │
         │  ✅ App works!                │
         └───────────────────────────────┘
```

---

## 🔍 Корневая причина (Code Flow)

### Проблемный код (ДО):

```javascript
// frontend/stores/game.ts
const getWsBase = () => {
  const config = getConfig();
  
  if (config.public.wsBase) {
    return config.public.wsBase;
  }
  
  if (config.public.apiBase) {
    return config.public.apiBase.replace(/^http/, 'ws');
  }
  
  if (typeof window !== 'undefined') {
    const protocol = window.location.protocol === 'https:' ? 'wss' : 'ws';
    return `${protocol}://${window.location.host}`;
  }
  
  // ❌ ПРОБЛЕМА: При SSR возвращается localhost
  return 'ws://localhost:3000';
};
```

### Что происходило:

```
Server-Side Rendering (Nuxt)
    │
    ├─► config.public.wsBase = undefined (не установлено)
    ├─► config.public.apiBase = undefined (не установлено)
    ├─► typeof window === 'undefined' (на сервере нет window)
    │
    └─► return 'ws://localhost:3000'  ❌
            │
            └─► Это значение попадает в HTML
                    │
                    └─► Browser получает localhost URL
                            │
                            └─► Попытка подключения к localhost
                                    │
                                    └─► ⚠️ Permission prompt!
```

### Исправленный код (ПОСЛЕ):

```javascript
// frontend/stores/game.ts
const getWsBase = () => {
  const config = getConfig();
  
  // 1. Приоритет: явно указанный wsBase
  if (config.public.wsBase) {
    return config.public.wsBase;
  }
  
  // 2. Конвертируем apiBase в WS URL
  if (config.public.apiBase) {
    return config.public.apiBase.replace(/^http/, 'ws');
  }
  
  // 3. В браузере определяем по текущему location
  if (typeof window !== 'undefined') {
    const protocol = window.location.protocol === 'https:' ? 'wss' : 'ws';
    return `${protocol}://${window.location.host}`;
  }
  
  // 4. ✅ Пустая строка вместо localhost
  // Browser detection сработает на клиенте
  return '';
};
```

### Что происходит теперь:

```
Вариант 1: Правильная сборка (npm run build:prod)
    │
    ├─► config.public.apiBase = "https://pokerscrum.ru"
    └─► return "wss://pokerscrum.ru"  ✅

Вариант 2: SSR fallback
    │
    ├─► config.public.apiBase = undefined
    ├─► typeof window === 'undefined'
    ├─► return ''
    │
    └─► В браузере:
            │
            ├─► typeof window !== 'undefined' ✅
            ├─► window.location.protocol = "https:"
            └─► return "wss://pokerscrum.ru"  ✅
```

---

## 📊 Сравнение команд сборки

### ❌ НЕПРАВИЛЬНО:

```bash
npm run build
```

```
Использует переменные из .env:
    │
    ├─► NUXT_PUBLIC_API_BASE=http://localhost:3000
    └─► NUXT_PUBLIC_WS_BASE=ws://localhost:3000
            │
            └─► ❌ localhost в production!
```

### ✅ ПРАВИЛЬНО:

```bash
npm run build:prod
```

```
Использует явно указанные переменные:
    │
    ├─► cross-env NUXT_PUBLIC_API_BASE=https://pokerscrum.ru
    ├─► cross-env NUXT_PUBLIC_WS_BASE=wss://pokerscrum.ru
    └─► nuxt build
            │
            └─► ✅ Production URLs!
```

---

## 🎯 Уроки на будущее

### ✅ Всегда делайте:

1. Используйте `npm run build:prod` для production
2. Проверяйте runtime config после сборки
3. Тестируйте WebSocket подключение
4. Не оставляйте hardcoded localhost в коде

### ❌ Никогда не делайте:

1. `npm run build` для production
2. Hardcoded localhost URLs
3. Деплой без проверки переменных окружения
4. SSR fallback на localhost

---

## 🔧 Быстрая проверка после деплоя

### 1. Проверить runtime config:

```javascript
// В браузере console:
const config = useRuntimeConfig();
console.log(config.public);

// Должно быть:
// {
//   apiBase: "https://pokerscrum.ru",
//   wsBase: "wss://pokerscrum.ru"
// }
```

### 2. Проверить WebSocket:

```
DevTools → Network → WS

Должен быть один WebSocket:
✅ wss://pokerscrum.ru/ws (Status: 101 Switching Protocols)

НЕ должно быть:
❌ ws://localhost:3000/ws
```

### 3. Проверить permission prompt:

```
Откройте https://pokerscrum.ru в новой вкладке

✅ НЕ должно быть запроса на доступ к локальной сети
✅ Приложение работает
✅ WebSocket подключен
```

---

## 📚 См. также

- [DEPLOY_HOTFIX.md](DEPLOY_HOTFIX.md) - инструкция по деплою
- [HOTFIX_LOCALHOST_ISSUE.md](HOTFIX_LOCALHOST_ISSUE.md) - детали
- [URGENT_FIX.txt](URGENT_FIX.txt) - быстрая шпаргалка
